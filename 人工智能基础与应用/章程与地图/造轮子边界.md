# 造轮子边界 · 谁写代码（本学科最高契约）

> 立约 2026-09-17，用户拍板口径："**分层双轨**"。
> 本学科同时要求两件互相冲突的事：① 唐杰作业要求"不会动手、只会调 API 的先下去"；
> ② 你的时间是稀缺资源，16 周内跑完全链路。**解决办法不是折中，而是划死边界**：
> 学习目标必须你手写；非学习目标必须我代写。边界之下，任何一方越界都算事故。

## 一、三条铁律

1. **手写区（`课题NN/手写/`）：Agent 禁止写入任何文件。** 不得给代码、不得给伪代码、
   不得把第三方实现（含 nanochat / CS336 参考实现 / GitHub 上的答案库）当答案指给你。
   允许的动作只有五类：解释报错、解释概念、审查你已写的 diff、建议 sanity check / toy example /
   profiler 检查、指路官方文档与讲义。
2. **脚手架区（`课题NN/脚手架/`、`环境/`）：Agent 全权负责且必须主动完成**，不等你催。
   这部分写一遍就够，重复劳动零学习收益。
3. **越界的正确姿势**：你说"给我看答案"→ 走提示阶梯（五级）→ 到第五级给答案后，
   **必须挂近迁移重考卡片**（隔 1–3 天、无提示、换场景再考）。给了答案不挂卡片 = 违规。

## 二、手写清单（学习目标，共 10 项）

| 编号 | 手写内容 | 为什么必须亲手写 |
|---|---|---|
| R1 | BPE 训练与编码算法（merge 循环、特殊 token、bytes 回退、流式编码） | 分词决定了模型看到的世界；面试/科研高频 |
| R2 | Transformer 全部组件（attention、RoPE、RMSNorm、SwiGLU、残差、初始化） | 梯度与形状直觉只从亲手写里长出来 |
| R3 | 训练循环与优化器（AdamW/Muon 更新式、LR schedule、梯度裁剪、精度管理） | 训练不收敛时，只有写过的人会 debug |
| R4 | 反向传播验证（gradient check、数值梯度对照） | 这是"我懂了"与"我能跑"的分界线 |
| R5 | **Triton attention kernel 本体**（分块、online softmax） | 唐杰作业②的核心；性能直觉的唯一来源 |
| R6 | Scaling law 拟合与外推（数据采集口径、拟合、预测、打脸记录） | 这是"科研判断力"的训练区，不能外包 |
| R7 | SFT / DPO / RLVR 的**损失函数与优势估计**（含 group normalization、重要性比） | 后训练是 2026 的重心；抄公式等于没学 |
| R8 | 数据过滤/去重的**判定规则**（启发式规则设计） | 数据决定上限；规则设计的品味靠自己做 |
| R9 | 可验证环境的**判定逻辑**（verifier / reward function） | 唐杰作业⑤的技术核心 |
| R10 | self-judge loop 的**协议设计**（何时判、判什么、如何防退化） | 唐杰课明确鼓励的方向；前沿无标准答案 |

## 三、代写清单（脚手架，Agent 负责，共 8 项）

| 编号 | 代写内容 | 交付形态 |
|---|---|---|
| B1 | 环境与依赖（uv / CUDA / 镜像 / 引导脚本） | `环境/bootstrap*.sh` + `uv.lock` |
| B2 | 数据下载/解压/分片/上传（含 Kaggle Dataset 打包） | `环境/数据/` 脚本 + 清单 |
| B3 | 日志、曲线绘图、实验编排、**断点续训封装** | `课题NN/脚手架/` 脚本 |
| B4 | 判分脚本（拉取 CS336 官方 `tests/` 并接入适配器） | `环境/judge/` + `judge.sh` |
| B5 | 证据带自动归档（run.yaml / 日志 / 曲线 / 产物哈希） | `课题NN/证据/` 由脚本生成 |
| B6 | 报告模板与排版（Markdown + LaTeX/NeurIPS 模板） | `环境/报告模板/` |
| B7 | 配额与超时治理（会话预检、超时自停、配额账本） | `环境/预检.md` + 脚本 |
| B8 | checkpoint 上传/下载与版本管理 | `课题NN/脚手架/ckpt_*.sh` |

## 四、协作区（一起定，你拍板）

| 编号 | 事项 | 分工 |
|---|---|---|
| C1 | 架构与底座选型、上游引入范围 | Agent 出方案与证据 → 你拍板 |
| C2 | 超参搜索空间、指标口径（bpb / CORE / 通过率） | Agent 出方案 → 你定口径 |
| C3 | 实验优先级与是否值得重跑 | Agent 给成本估算 → 你决定 |
| C4 | 可验证环境的题目设计 | 你出设计意图 → Agent 评估可行性与成本 |

## 五、违规示例（判例，会长大）

- **E-01（预防性记录）**：Agent 往 `手写/` 里写入实现文件 —— 即使内容正确也算违规，
  必须改为"审查意见 + 测试建议"落到 `判卷记录.md`。
- **E-02（预防性记录）**：Agent 说"你可以参考 nanochat 的 `flash_attention.py` 第 30 行" —— 违规。
  正确做法：说明该文件**在整体架构中的位置与接口契约**，指出官方 Triton 教程与 CS336 A2 handout 的对应章节。
- **E-03（预防性记录）**：Agent 帮你把实验卡写好之后直接开始烧 GPU 时间 —— 违规。
  实验卡必须由你确认（哪怕只是回一句"跑"）。

## 六、边界自检（每次动手前问一遍）

1. 我现在要写的这个文件，属于 R1–R10 还是 B1–B8？
2. 如果是 R 类 → 我只能读、只能评论，不能写。
3. 如果是 B 类 → 我必须写，且要写完整（不留 TODO 给用户）。
4. 如果两边都不是 → 进协作区，先出方案再动手。
