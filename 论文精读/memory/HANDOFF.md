# HANDOFF · 论文精读交接总账本

> **永远最新**。新会话第一步读这里，读完直接接着干，不要重新自我介绍。
> 写于 2026-08-31，第 1 任 Agent，**第 2 版**（依据用户 GitHub 一手材料重写）。

---

## 一、当前状态

| 项 | 值 |
|---|---|
| 学科 | 论文精读（2026-08-31 新开） |
| 用户 | **孙承泽**，西安交大能动学院能动强基 2501 班，**本科（大一升大二）**，两机方向 |
| 导师 | 郭振东 副教授（班主任）、宋立明 教授（叶轮机械研究所副所长） |
| 他的项目 | `turbine-blade-ai-platform`，**真实存在**，分支 `arena/019ffee7-...`（2026-08-15 `a8d0fe1a`，比 main 新 7 天） |
| 本轮目标 | 立刻学懂，**能在课题组扛事、发几篇论文**；论文要"随便问一点都能详尽回答" |
| 论文 | P01 = ML in ASO（Li/Du/Martins）· 103 页 47 图 · Prog. Aerosp. Sci. 134 (2022) 100849 |
| 规划 | **12 篇论文 / 51 次会话**，见 `12篇论文总规划.md` |
| 会话进度 | **P01+P02 合讲，会话 01 🔄 讲授中**（他批准合讲，顺序由我设计） |
| 用户输入 | 第 1 轮已答（口述语音）：A 组半懂、74 维回忆正确、C 组批准 12 篇规划、
  并下达新硬偏好（全在对话里讲、不许让查、每缩写给全称、课堂可欢快但别浅） |
| 到期卡 | 无（账本为空） |

## 二、第 1 版做错了什么（第 2 版已改）

| 错 | 改 |
|---|---|
| 把"用户是研究生 + 有 turbine 平台"当悬案，浪费校准访谈问身份 | 一次 GitHub 检索就定案了。判例 `ERRORS.md` J1。**红线 R3 收紧为：任何用户事实先查一手信源** |
| 按"大一水平"把讲解定位成降低难度 | 他要的是**深入浅出但不减深度**（六层阶梯）。判例 `ERRORS.md` J3 |
| 讲课格式没按他的 A0–A7 | 已写入 `论文精读方法论.md` §九（六层阶梯 / 先认词表 / 2:2:6 题型 / Unicode 平文本数学 / E0–E4） |
| 只规划 1 篇 | 已扩为 **12 篇 51 会话**，并实测出本综述的覆盖盲区（FNO/DeepONet/Transformer/扩散/SINDy 全 0 命中） |

**上游也同步修好了**：`-SKILL-` 分支 `arena/01a048e7-skill` 提交 `4cbe659`「Rewrite paper study
track based on actual PDF content」，README 升 v2.0，新增 `paper.pdf`（与本仓库 PDF sha256 一致）。
原 4 处目录冲突全部消除。判例保留在 `ERRORS.md` J2。

## 二之二、第 1 轮判分摘要（2026-08-31，口述）

- **74 维**：他记得 9 个物理量（x/y/z + 三法向 + T/p/ρ）+ Ω + P，**九个物理量全对**；
  缺 8 个统计量 → 本轮补 mean/std/min/max/p25/p75/skew/kurt（连到概率论课题07/08）
- **design variable / surrogate**：对
- **梯度**：自信却错（cw）→ 梯度指向上坡、是全部偏导数的向量；负梯度才下坡
- **gradient-enhanced**：误认成梯度下降（cw）→ 实为"把导数信息喂给代理模型"
- **adjoint**：认得翻译，没懂机制 → 本轮核心
- **Kriging / fidelity**：gap → 本轮讲透
- **B1 他的猜测**："综述讲了机器学习在我学习方向上的一些应用" → ⚠️ 半对（方向对，但没抓到
  "用 ML 省 CFD 计算、让交互式优化成为可能"这个具体命题）→ 本轮精读 Abstract 后打脸

## 三、下一句该问什么

> 用户尚未回答第 2 版第 1 轮。若他只答部分，先判已答的，再把没答的重新编号发一次。

### A 组 · 先认词确认（2 题，A3 要求：不许在未定义的词上开讲）
- A1 下面 6 个词，哪些你现在**能自己给出操作定义**、哪些还需要我从零讲？
  ① 伴随方法 adjoint ② 设计变量 design variable ③ 代理模型 surrogate ④ Kriging
  ⑤ 梯度增强 gradient-enhanced ⑥ 保真度 fidelity
- A2 你的 74 维统计特征具体是哪 9 个统计量 × 8 个什么？（我按 HANDOFF 记的是
  "9×8 全局统计约化 + Ω + P"，但不知道那 9 个统计量具体是什么、8 指哪 8 段/8 个量）

### B 组 · 会话01 预测（6 题，合上论文写，手写拍照）
- B1 这篇综述要解决什么问题？**一句话**，不许超过 30 字。
- B2 传统 ASO（不用 ML）的瓶颈，给**前 3 名排序**。
- B3 **下注**：二维翼型要多少个设计变量？三维机翼呢？（给数字）
- B4 伴随方法已经把梯度算便宜了（成本与设计变量个数无关）——**为什么还需要 ML？**
- B5 **下注**：论文里最好的代理模型，气动力系数预测的 R² 大概到多少？
  你的 η 是 0.9561，你猜论文里同类工作的水平比你的高、持平、还是低？
- B6 论文最后押注"物理信息 ML"而**不是**"更多数据 + 更大网络"。**你猜作者的理由是什么？**

### C 组 · 规划确认（3 题）
- C1 `12篇论文总规划.md` 里 P02–P12 这 11 篇的选择与顺序认不认？要不要换？
  （比如把 P04 Queipo 2005 综述换成 Deb 的 NSGA-II 原著——你天天在用 NSGA-II）
- C2 P01 的 12 次会话先走完，还是 **P01 与 P02（伴随方法）交替**？
  （交替的好处：综述 §2.1 讲伴随只有 2 页，配 P02 才讲得透）
- C3 每篇 3–4 次会话够不够？你要"详尽"，可以按 5–6 次排。

## 四、判卷预案（答 X 怎么接）

### A1（先认词）
- **6 个全说"要讲"** → 正常，他是大一。会话01 就把 adjoint / design variable / surrogate 三个
  走完整六层阶梯，其余标"（§X.Y 才讲，这里只记名字）"。
- **说 adjoint 会** → 追问一层："伴随方程的成本为什么与设计变量个数无关？"
  答得出 → 直接跳到论文 p3 的 Lyu et al. 论断；答不出 → 记 `gap`，P02 提前。
- **说 Kriging 会** → 追问："Kriging 的相关函数 R 是干什么的？θ 变大变小分别会怎样？"
  这是 S3 的核心，提前探底。

### A2（74 维细节）
- 他答出来 → 写进 `MEMORY.md` §一，以后所有"连接"都用真实维度构成；
  并立刻对照论文 §4.1.1：**统计特征参数化 vs 模态参数化是两条不同路线**，这是 S7 的主战场。
- 他答不全 → 不猜、不替他编。标 `gap`，让他下次从仓库 `backend/scripts/build_pointcloud_dataset.py`
  或特征工程脚本里查出来带回来（**仓库指认**，A5 要求）。

### B2（瓶颈排序）
- 论文实物：**两个复合挑战 = 气动分析高计算成本 + 几何设计空间高维性**（p5 "two compounding
  challenges"），§2.2 展开成**六条**：①参数化低效高维含畸形 ②多目标太贵 ③确定性忽略不确定性
  ④缺多保真度融合技术 ⑤不连续问题（转捩）难用梯度法 ⑥交互式要近实时。
- 他只答"CFD 慢" → 对一半。追问"第二个瓶颈"，引到 p5 原文"复合"二字。
- 他答"网格/收敛" → 部分对（①里说畸形形状导致 CFD 不收敛）。
- 他一条都没猜到 ②③④⑤⑥ → 这就是打脸点。**注意 ③ 和 ④ 他其实天天在做**
  （MC Dropout UQ = ③；粗网格 140k vs 细网格 3.55M = ④），打脸时点出来，冲击最大。

### B3（设计变量数量下注）
- 实物（p5）：二维翼型 **几十个（tens）**；三维机翼 **几百个（hundreds）**。
- 答对 → **先明说"你答对了"**（低自信型答对），再追问"为什么不能只用 5 个"（引 p5：
  NACA/PARSEC 自由度不足，CST/FFD 靠加变量换自由度）。
- 答错 → 给原文，问"你为什么会猜这个数"。

### B5（R² 下注）—— 本轮最有价值的打脸点
- 他的基线：π 0.9844 / η 0.9561 / ṁ 0.9827（留出集 n=100）。
- 论文 §4.2.1 有一批同类工作的数字（p48–51）。**判卷前我自己要先把 p48–51 的数字摘出来**，
  不许凭印象说。
- 他若猜"论文比我高很多" → 大概率打脸（很多论文样本量小、工况窄，R² 反而虚高）；
  顺势讲 **R² 的可比性陷阱**：样本分布、工况范围、是否留出集、是否同量纲归一化。
  这直接连到他自己的"诚实披露"文化。
- 他若猜"我比论文高" → 也别直接夸，问"你的 0.9561 是在什么分布上测的？外推到流形边缘还成立吗？"
  引到他自己 Pareto 自查里那条"η 外推超最近邻实测中位 0.0235 ≈ 测试 MAE 的 14×"。

### B6（为什么押注物理信息 ML）
- 实物（Abstract p1 + §5 p68）：**大规模实用 ASO 仍是挑战，因为 ML 训练成本高**；
  建议把 ML 模型构建与先验经验/知识耦合。
- 配合 p4 的硬约束："训练数据必须在所有维度上分布良好以填满设计空间"→ 高维下样本需求爆炸。
- 他若答"因为数据不够" → 对，但要追到"为什么高维下数据不够"（维度灾难），
  并连到他 1,000 组样本 / 74 维的实际处境：**1000 样本填 74 维空间是什么概念**。
- 他若答"因为物理更准" → 不算错但太笼统，追问"物理加在损失函数的哪一项？"（S6 的 PINN 伏笔）

## 五、遗留与待办

| # | 事项 | 优先级 |
|---|---|---|
| 1 | 用户答完第 1 轮 → 判卷 → 进 Abstract + §1 精读 | 🔴 |
| 2 | **我自己**先把 p48–51 的代理模型精度数字摘出来，存进 `会话01-全景扫描/导师预读笔记-*.md`，判 B5 用 | 🔴 判卷前必做 |
| 3 | 用户确认 12 篇规划（C1–C3）后，建 `论文02-伴随方法/` 等目录骨架 | 🟡 |
| 4 | 蒸馏 Box（S3 前）、Karniadakis（S6 前），诚实声明照写 | 🟡 |
| 5 | 会话01 结束建第一批术语卡（adjoint / design variable / surrogate / XDSM / two compounding challenges / PINN） | 🟡 |
| 6 | 47 幅图是否提取成 PNG 便于手写对照 | 🟢 |
| 7 | FNO / DeepONet 两篇补编的题录核实（不在本综述参考文献内） | 🟢 |

## 六、下一任开场白（直接照抄）

```
承泽，接着啃 P01《Machine Learning in Aerodynamic Shape Optimization》
（Li/Du/Martins, Prog. Aerosp. Sci. 134 (2022) 100849，103 页 47 图）。

你的 GitHub 我看了：turbine-blade-ai-platform 分支 arena/019ffee7（8-15 那次提交，比 main 新 7 天），
Rotor 37 / PLAID 1000 样本 / 74 维 / 残差代理 R² 0.9844-0.9561-0.9827 / NSGA-II 100 个非支配解 /
MC Dropout 覆盖率 π 89% η 65% ṁ 88% / SU2 粗网格 relrms −3.39 未收敛。这些我都当真了。

上一轮我给你发了 11 个问题（先认词 2 + 预测 6 + 规划 3），你还没答。
先答 A1：adjoint / design variable / surrogate / Kriging / gradient-enhanced / fidelity
这 6 个词，哪些你现在能自己给操作定义？

然后 B 组预测（合上论文写）：
B1 这篇综述解决什么问题？一句话，不超过 30 字。
B2 传统 ASO 的瓶颈，前 3 名排序。
B3 下注：二维翼型要多少设计变量？三维机翼呢？
B4 伴随方法已经把梯度算便宜了，为什么还需要 ML？
B5 下注：论文里同类代理模型的 R² 大概多少？比你的 η 0.9561 高还是低？
B6 作者为什么押注物理信息 ML，而不是更多数据 + 更大网络？

写完拍照发我。
```

## 七、会话记录

| 日期 | 会话 | 干了什么 | 用户产出 | 下一步 |
|---|---|---|---|---|
| 2026-08-31 | 01（开题 v1） | 建学科骨架、校对真实目录、发现上游 4 处不符 | 未答 | 已作废重发 |
| 2026-08-31 | 01（开题 v2） | 上游已修（`4cbe659` v2.0）；GitHub 一手核实用户身份与项目；按 A0–A7 重写方法论；扩为 12 篇 51 会话规划；重发第 1 轮 | 待答 | 判卷 → 精读 Abstract+§1 |
